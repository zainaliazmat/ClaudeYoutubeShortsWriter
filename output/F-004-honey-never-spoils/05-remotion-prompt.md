# F-004 — Remotion Composition Prompt

## Use the Remotion official skills to build this composition.
- Composition id: `F-004-honey-never-spoils` · 1080×1920 · 30fps · durationInFrames: **915** (from `vo-timing.json` `total`)
- Duration via `calculateMetadata` reading `vo-timing.json` `total` — never a hardcoded const.

effective_style: kinetic-typography

## Design tokens
- **Fonts:** Anton 400 (display/hero — `@remotion/google-fonts/Anton`); Archivo Black 900 (stat chips/fragments — `@remotion/google-fonts/ArchivoBlack`); Space Mono 400 (tabular, only if a count-up is used; letter-spacing −0.04em ≥120px).
- **Colors:** bg gradient `#2B1200` (top) → `#5C2800` (bottom); radial glow `#8B4A00` @30% ellipse ~800×1100 centred ~40% down; hero/primary `#FDF3DC`; accent/chips `#F5B731`; reveal/payoff `#FFD966`; caution `#E07B39`.
- **Persistent background layer (ALL frames):** the vertical gradient + radial glow (+ optional 3% feTurbulence grain on an `#F5B731` layer). NEVER flat single-hex black.
- **Persistent motif (ALL frames):** centred SVG honey-jar silhouette ~500–600px tall (center/lower-center band), dark fill `#3D1A00` + lit amber stroke `#F5B731` @60% from frame 0 (reads as an intentional shape, not dead silhouette). Inline `<svg>` bezier path — no logo/trademark/photo.
- **Motion signature:** hero word slam-in = `spring({stiffness:220, damping:18})` scale 0.6→1.0 over ~8f; stat-chip pop = easeOutBack scale 0.5→1.05→1.0 over 10f; reveal wipe = left→right mask 12f easeOut; jar fill = animated clipPath 0→100% height 24f easeOut; loop = cross-dissolve. No `Math.random()` — use Remotion `random(seed)`.

## Scenes (frame-exact — tiles [0,915] contiguous half-open)
> Bands: context = upper third (y≈260–620) · hero + jar = center (y≈620–1180) · stat chips = lower third (y≈1180–1500) · captions = y≈1560–1660 (their own lane). Hero ≥ ~280px Anton. No dead zone >40%.

### Scene 0 — Hook (frames 0–109)
- Hero: `ONE FOOD` (Anton ~320px, `#FDF3DC`, upper-center) + `NEVER EXPIRES` (Anton ~260px, `#F5B731`, just below). **Present at FULL opacity at frame 0 (thumbnail — NO fade-in).** Slam-in is for the loop-return only, not frame 0.
- Jar silhouette present behind from f0.
- Frame-fill note: hero fills center band, jar anchors lower-center, gradient+glow fills the rest. ~0% dead zone.

### Scene 1 — Beat 1 (frames 109–287)
- `HONEY` slams in at f≈111 (Anton ~380px, honey-gold `#F5B731`, center). Jar interior fills with animated gold clipPath bottom→top f≈115–139. Stat chip `~0% water` (Archivo Black, lower band) pops at f≈194.
- Frame-fill note: hero center, jar fill is the motion, chip lower band.

### Scene 2 — Beat 2 (frames 287–452)
- Two stat chips stagger in (easeOutBack, 6f apart): `ACIDIC` (left) + `+ H₂O₂` (right), lower-center. `MICROBES DIE` (Archivo Black, `#FDF3DC`) wipes in below at f≈406.
- Frame-fill note: chips occupy center/lower bands; jar persists; upper band carries a small `WHY?` or stays clean.

### Scene 3 — Beat 3 — REVEAL / PAYOFF (frames 452–638)
- Upper band context: `ANCIENT EGYPT` (Archivo Black, `#F5B731`) in by f≈463; `1000s OF YEARS OLD` (Anton ~260px center) by f≈569.
- `STILL PRESERVED` (Anton ~290px, reveal/payoff `#FFD966`) **reveal-wipe left→right, 12f easeOut, starting f=611** (the spoken word "preserved").
- **Lottie accent: `accent-beat3.json` · placement: above-captions (centered upper-center, ~x540/y420 from top-left) · sizePx: 320 · frame window: [611, 638) within this beat · fps: 30.** The success-check draws in starting f611 (synced to "preserved"), completes ~f633, holds to beat end. Clear of the caption lane and the very top. This is the confirmation verdict — the accent IS the payoff visual.
- Frame-fill note: context upper, hero+check center, jar lower-center; balanced, no dead zone.

### Scene 4 — Beat 4 — Caveat (frames 638–840)
- `KEEP IT SEALED` (Anton, `#FDF3DC`) with a lid-snap (scale bounce 1.0→1.15→1.0 over 8f) at f≈681 ("sealed"); a jar-lid SVG seats on the jar neck. `CRYSTALS ≠ SPOILED` (Archivo Black, caution `#E07B39`, lower band) in by f≈779. Begins settling toward the hook layout by f≈820.
- Frame-fill note: hero center, lid motion on the jar, caution chip lower band.

### Scene 5 — Loop-back (frames 840–915, silent tail)
- Cross-dissolve (opacity 1→0 over f840–870) back to the Hook hero `ONE FOOD / NEVER EXPIRES`, fully present by f≈885, **identical composition/colors/position to frame 0** so the auto-loop is invisible. VO has ended; music fades to 0.

## Captions
- Burned-in, word-by-word, **generated by codegen from `vo-timing.json` integer word frames** (display string per word; merge consecutive words sharing a `display`). Lane: y≈1560–1660, clear of bottom ~15% and the very top — never collides with hero type, the jar, or the Lottie check (which sits in the upper-center band).

## Audio (VO is the lead — music ducks under it)
- **VO:** `staticFile('vo.wav')` @ vol ~0.95 — the lead (starts f0).
- **Music:** `staticFile('music-inspiring-cinematic-ambient.mp3')` (Pixabay Content License, no attribution required) with a frame-callback `volume` reading the `vo-timing.json` `envelope` (base ~0.72 → ~0.22 under the speech region, swelling on the beat3 payoff after speech). Fade in ~f0–15; fade out to 0 across the silent loop tail (f840–915).
- **SFX (`staticFile`):** `sfx-reveal-bloom.mp3` @ f611 vol ~0.65 (warm bloom as `STILL PRESERVED` wipes in); `sfx-soft-chime.mp3` @ f620 vol ~0.75 (the confirmation chime as the Lottie check completes); `sfx-lid-pop.mp3` @ f681 vol ~0.50 (lid snap). Use `volume={() => n}` (eslint `@remotion/volume-callback`).
- **Master (final step):** master the rendered file to **−14 LUFS / ≤ −1 dBTP** (default linear-gain + true-peak limiter, LRA-preserving for voice-led; `scripts/master.mjs`), then verify.

## Loop-back
- Final frame (915→0) matches frame 0: same `ONE FOOD / NEVER EXPIRES` hero, same gold, same position, same jar — invisible seam. Hook hero present at full opacity at BOTH ends (no fade-in at frame 0).

## Assumptions made (where the script/assets were vague)
- Caption lane fixed at y≈1560–1660; Lottie check placed upper-center (x≈540/y≈420) so it never shares a horizontal band with captions or the lower stat chips.
- `H₂O₂` used as the compact chip for "hydrogen peroxide" (the narration says it in full; caption shows the spoken words).
- `1000s OF YEARS OLD` framed exactly per the verified claim ("thousands of years old, still preserved") — no specific age, no King Tut (abstain per 01-verified-facts.md).
