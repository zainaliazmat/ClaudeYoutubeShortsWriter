// Type declaration for quality-floors.mjs so the render-lib tsc gate can resolve the
// cross-boundary import in quality-floors-agree.test.ts (the engine source is plain JS).
export const BG_STOP_LUMA_MIN: number;
export const COMPOSITED_LUMA_MIN: number;
