# Cross-run lessons — `/short` pipeline

Dated, specific, actionable learnings. The `/short` run reads this at the start (also injected via the SessionStart hook) and `short-assembly` appends to it at the end. Keep each entry to one concrete line.

<!-- format: - F-NNN: <what happened> → <what to do next time>. YYYY-MM-DD -->

- Seed: keep facts to ONLY what's in `01-verified-facts.md`; the writer must not invent claims. 2026-06-16
- Seed: prefer YouTube Audio Library for music (no Content-ID claims); record the exact CC-BY credit string when a track requires it. 2026-06-16
- Seed: frame ranges are half-open and 0-indexed (`next.start == prev.end`); trust the reviewer's validator over manual math. 2026-06-16
- Pipeline note: newly-added `.claude/agents/*` (e.g. asset-scout) don't register in an already-running Claude Code session — reload Claude Code before relying on a new subagent. 2026-06-16
- F-001: deep-research's adversarial verify earned its keep — it killed the "pyramid completed 2580 BC" (that's the START) and the AERA "85 yrs / 2504 BC" misread; keep ~2560 BC for completion. 2026-06-16
- F-001: for "X is closer to us than to Y" time-gap facts, anchor the comparison on Cleopatra's BIRTH (69 BC) for the cleanest ~2,500-vs-~2,000 framing, and NEVER show her death day (Aug 10 vs 12 is genuinely disputed) — years only. 2026-06-16
- F-001: deep-research workflow journal can go stale / TaskOutput 404 before delivering a synthesized report — recover by extracting the per-claim verify results (verbatim quote + source URL + refuted flag) straight from journal.jsonl. 2026-06-16
- F-001: number-reveal typography reads best with a monospace digit face (Space Mono) so count-up animations don't jitter width; pair with a heavy grotesque display face. Reusable Fathom signature. 2026-06-16
- F-001: shipped at **-30.6 LUFS** (near-silent in-feed) — the spec ducked the bed to `volume=0.11` "under dialogue" but these videos have NO VO. Fix: no-VO ⇒ music bed is the LEAD (~0.7), and ALWAYS master the render to **-14 LUFS / ≤ -1 dBTP** via two-pass `loudnorm` + verify. YouTube never boosts quiet masters. See `asset-sourcing/references/audio-mastering.md`. 2026-06-17
- F-001: frame 1 was a ~0.3s black fade-in → weak thumbnail/first-frame. Open directly on the hook (no black lead-in); first frame must be legible. 2026-06-17
- F-001 (render review): a video can pass 92/100 and still look flat — the reviewer graded spec completeness + frame math with NO visual-design dimension. Added rubric **Category 9 — Visual Design Quality & Frame Utilization** (frame utilization, hero scale, decorative-layer visibility, core-mechanic legibility, scale-honest data viz, color energy); 90+ now requires Cat 9 ≥ ~70%. Rebalanced weights to 100. 2026-06-17
- F-001: asset-sourcing literally said "Default to NO imagery" → tiny invisible icons + flat near-black bg. Now: earn the frame — depth background (gradient/grain/nebula, never flat single-hex), ≥1 large motif (400–700px), hero type ≥ ~280px, high-impact display face; tighten mono tracking on big numbers. 2026-06-17
- F-001: layout clustered everything at y≈560–960, leaving the lower ~50% empty. prompt-generator now requires laying out the FULL safe area (y≈260–1660) in explicit upper/center/lower bands with a per-scene frame-fill note; flag any beat >40–60% dead space. 2026-06-17
- F-001: comparison brackets drawn ~3:1 for a real ~5:4 gap, contradicting "~450 years closer", and the count-up rendered NEGATIVE interim values. Rule: compute data-viz positions FROM the values (scale-honest) or label the exaggeration; clamp count-up displays ≥ 0; count-ups ~0.8–1.2s ease-OUT then hold. 2026-06-17
- F-001: reviewer ran on 02-script.md only, but visual decisions live in 03/05 (ungraded). Step 6 now reviews 02 AND 05 together (Cat 9 grades the composition spec). 2026-06-17
- F-001: nothing inspected rendered pixels → 833-vs-840 frames, text/glyph collisions, invisible brackets, and ~9–14/255 mean luma (near-black, 2× ~6s black stretches) all shipped. Added **render-qa** skill (step 8, post-render): duration, loop seam, brightness/black-screen, per-beat dead-space/collision/mechanic, loudness — routes failures back to the responsible spec file. 2026-06-17
- Writer: identify any potentially-unfamiliar subject in ≤4 words within the first two beats (role, not bio; accuracy per the facts file); default to a declarative hook over "Did you know…", and re-run the validator after any hook-word change. 2026-06-17
- Housekeeping: scriptwriter-skill was a dead fictional-screenplay skill (NOT the /short writer); first quarantined via description + name, then **deleted** in QA round 3 as unused dead weight. Step 3 always uses youtube-shorts-writer. 2026-06-18
- F-001 v3: regenerated through the upgraded pipeline (steps 3–7, reused 00/01 facts). Biggest visual win = flipping the timeline from HORIZONTAL (wasted the tall 9:16 frame) to VERTICAL (pyramid top → moon bottom, fills full height) — and it makes "closer to the Moon" a spatial fact (Cleopatra node at the time-accurate 55% down). Reusable for any time-gap comparison. Scored 93/100 on the new 9-cat rubric (Cat 9 = 89%). 2026-06-17
- F-001 v3: a vertical timeline makes data-viz scale-honesty trivial — compute px/yr (span 4,529yr / 1,200px = 0.265) and place every node from its date; the two gap segments then come out proportional (660:540 = 1.22 = 2,491:2,038) for free. Keep the frame map unchanged across a visual-only re-spec so the already-mastered audio cues still line up. 2026-06-17
- TTS engine: Kokoro 0.9.4 exposes per-WORD `.start_ts`/`.end_ts` on `result.tokens` (verified), but emits punctuation (`.`/`,`) as separate timed tokens — filter to alnum word tokens or the §3.6 token-count check trips the fallback. And `silenceremove` with `stop_periods=1` strips INTERIOR pauses (collapsed 12.35s→0.38s, coverage 716%); trim ends only via the `areverse` double-pass. espeak-ng ships in the `espeakng-loader` wheel misaki pulls in — no apt/sudo; set `PHONEMIZER_ESPEAK_LIBRARY` from it. PEP-668 distros need a venv (`.venv-tts`, gitignored). 2026-06-17
- F-001 v4: went VO-driven (Kokoro `am_michael`). At speed 1.0 the ~31s narration ran 40.6s — number expansion ("2,500"→4 spoken words, "BC"→2) inflates spoken-token count well past the display words, so SIZE the narration in spoken tokens, not display words, and use `--speed 1.08` to land 28–32s (overrun edge). Continuous narration merges into ONE speech_region, so the bed stays ducked the whole VO and swells only on the silent tail — correct, not a bug. 2026-06-17
- Pipeline (VO): the validator now reads `total` from `vo-timing.json` and tiles the frame-map TABLE the engine patches between `<!-- FRAME-MAP -->` markers — run it on the script IN the run folder (a temp-file copy loses the sibling json). Captions: merge consecutive `vo-timing.json` words sharing a `display` into one token (a number/abbrev spans several spoken-word frames but renders once). 2026-06-17
- MERGE (Phase 0): the Remotion renderer now lives at `render/` in this repo (monorepo). `node_modules`/`.venv-tts` are gitignored, rebuilt by `scripts/bootstrap.sh`; `make doctor` asserts ffmpeg/node/espeak/venv. `render/public/` is copy+clean (gitignored, run-scoped) — `scripts/seed-public.sh` repopulates it from `output/F-NNN/` (canonical: `vo.wav` at run root, music/SFX in `output/F-NNN/assets/`). 2026-06-17
- MERGE (Phase 1): `node scripts/render-run.mjs output/F-NNN` = render+master in one shot (validate id, asset gate, seed public, `npx remotion render` via execFile+hard timeout, two-pass loudnorm → `final.mp4` verified -14 LUFS/≤-1 dBTP). Missing music/SFX → HALT with a download checklist (owner=human, the loop can't fetch binaries). `assets.json` (schemaVersion 1) in the run folder lists the required binaries. 2026-06-17
- MERGE (Phase 1 master): two-pass loudnorm `linear=false` lands -14 LUFS but collapsed F-001's LRA to ~3 (target was 11). The gate only checks I/TP; if dynamics matter, measure LRA and consider `linear=true` or skipping the master on already-close mixes. Not a blocker — loudness is correct. 2026-06-17
- MERGE (Phase 2): codegen writes per-video scene `.tsx` under `render/src/F-NNN/` on the shared `render/src/lib/` (motion, Background, Captions, AudioBed, safeArea, tiling, captions-core — extracted from F-001). Duration MUST come from `calculateMetadata` reading `vo-timing.json` `total` (never a `DURATION` const — the F-001 v1 bug). Scene boundaries live in `scenes.json` (single source; `data.ts` derives `SCENES`, `check-tiling.mjs` reads the JSON via fs — importing `data.ts` at runtime fails on its Remotion font imports). Gate: `npm run gate` (tsc+eslint) + `npm run test:lib` + `check-tiling.mjs`. 2026-06-17
- MERGE (Phase 2 tooling): lib pure logic is TypeScript tested with `node --experimental-strip-types --test` (Node 22.18, zero new deps); `.ts` test imports need the explicit `.ts` extension, so tsconfig needs `allowImportingTsExtensions` (ok under `noEmit`). eslint's `@remotion/volume-callback` rule rejects `volume={number}` on `<Audio>` — use `volume={() => n}`. 2026-06-17
- MERGE (Phase 2 caveat): a brand-NEW skill (`remotion-codegen`) usually needs a Claude Code reload before `/short` auto-invokes it as a registered step — same as new `.claude/agents/*`. (Observed: it registered mid-session in this harness, but don't rely on that.) 2026-06-17
- F-001: near-black/flat background in the video stage — auto-fixed by regenerating the bg gradient tokens from the 05-remotion-prompt palette (#0B1430->#1C2A55). Codegen must always use the lib Background with the spec gradient, never a flat/dark single hex. 2026-06-17
- MERGE (capstone): a FRESH topic (F-002, T. rex vs Stegosaurus) ran the merged factory end-to-end — niche-memory → verified facts → writer → Kokoro VO (720f) → asset/audio spec (reused F-001's licensed Pixabay binaries, same channel/license) → codegen render/src/F-002 on the shared lib → render → master → QA loop → PASS (score 94, Cat9 98, -14.01 LUFS) with ZERO hand edits. Same-archetype reuse (vertical timeline, 3 nodes, 2 scale-honest gaps) made codegen first-try-clean. 2026-06-17
- MERGE (loop seam): F-002 passed with one minor warning — qa-probe loop-seam YUV delta 7.2 (>6) because the persistent Timeline shows lit nodes + full bars at the tail but is empty at frame 0; the LoopBack cross-dissolve only covers the text layer. For a perfect seam, codegen should freeze/reset persistent furniture (timeline/bars) to its frame-0 state during the loop tail, not just dissolve the text. Non-blocking but a real Cat-9/seam refinement. 2026-06-17
